import data from "../../data/programData.json";
import styles from "./ProgramDay.module.scss";

const ProgramDay = () => {
  return <h1 className={styles.heading}>TEST</h1>;
};

export default ProgramDay;
